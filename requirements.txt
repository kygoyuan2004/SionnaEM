# SionnaEM — Micro-Doppler Validation with Sionna RT
# ===================================================
# Conda environment: sionna_rt
# Python: 3.11
#
# Install: pip install -r requirements.txt
# Note: sionna-rt requires CUDA-enabled GPU for ray tracing acceleration.

# Core framework
sionna==2.0.1
sionna-rt==2.0.1
tensorflow==2.20.0
keras==3.14.1

# Ray tracing backend
mitsuba==3.8.0
drjit==1.3.1

# Scientific computing
numpy>=2.0
scipy>=1.15
matplotlib>=3.9

# Optional — PDF report generation
weasyprint>=68
markdown>=3.10

# Optional — environment verification
# torch (PyTorch) — already included as sionna dependency, version ~2.11
